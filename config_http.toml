[[iris]]
name = "local"
server = { host = "localhost", port = 1972, username = "SuperUser", password = "SYS" }
endpoints = [
    {path = "/mcp/warehouse" }
]

[mcp]
transport = "http"
port=8080
host="127.0.0.2"
